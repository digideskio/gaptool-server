gaptool:
    build: .
    links:
      - redis
    ports:
      - "3000:3000"
redis:
    image: redis:2.8.13
    ports:
      - 6379
    volumes:
      - redis_data/:/data
    command: redis-server --save 60 100

